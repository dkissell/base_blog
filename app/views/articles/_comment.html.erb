<div class="comment">
  <h4>Comment by <%= comment.author_name %></h4>
  <p><%= comment.body %></p>
  <p>Posted <%= distance_of_time_in_words_to_now(comment.article.created_at) %> later</p>
</div>

  
  